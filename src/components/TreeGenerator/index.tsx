import React from 'react';
import {ITodoItem} from "../../interfaces/ITodoItem";

type Props = {
    treeElements: ITodoItem
};

export const TreeGenerator = ({ treeElements }: Props) => {
    return (
        <div>

        </div>
    );
};